{{#each this}}
<div class="comment">
    <section class="comment">
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="comment_text">
            {{comment_text}}
        </div>
    </section>
</div>
{{/each}}
